{/* DO NOT EDIT! */}
{/* Automatically generated from docusaurus-template-liquid/templates/docusaurus. */}

import CodeBlock from '@theme/CodeBlock';

{/* ------------------------------------------------------------------------ */}

## Install

The package is available as
[`@xpack/doxygen2docusaurus`](https://www.npmjs.com/package/@xpack/doxygen2docusaurus/)
from the public [`npmjs`](https://www.npmjs.com) repository;
it can be added as a
dependency to any TypeScript or JavaScript
project.

The command to install the
**latest** available version of **@xpack/doxygen2docusaurus** is:

```sh
npm install @xpack/doxygen2docusaurus@latest
```

To install a **specific version**, mention it explicitly:

<CodeBlock language="sh"> {
`npm install @xpack/doxygen2docusaurus@1.0.1
`} </CodeBlock>

The package does not provide any executables, and generally there are **no
reasons to install it globally**.

## Uninstall

As any **npm** package, **@xpack/doxygen2docusaurus** can be uninstalled
from the project and the dependency removed from `package.json` with:

```console
npm uninstall @xpack/doxygen2docusaurus
```
